/**
 * Gig creation route at /gig/create.
 *
 * Re-uses the full Create Gig form from the tabs version to ensure
 * both entry points (the + tab and this route) are identical.
 */
export { default } from '@/app/(tabs)/create';
